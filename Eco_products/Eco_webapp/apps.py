from django.apps import AppConfig


class EcoWebappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Eco_webapp'

    # def ready(self):
    #     import Eco_webapp.signals  # Import signals to ensure they're registered


    
